use_default_settings: true

general:
  debug: false
  instance_name: Shell Search

search:
  safe_search: 1
  formats:
    - html
    - json

server:
  secret_key: shell-local-prototype
  limiter: false
  image_proxy: false
